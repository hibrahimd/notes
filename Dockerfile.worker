FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

# Generate Prisma client
RUN npx prisma generate

# Create storage directory
RUN mkdir -p /data/notal-storage

ENV NODE_ENV=production

CMD ["npx", "tsx", "src/worker/index.ts"]
